// let number = $("input#number").val();
let inputNumber
let inputArray = [];

for (i = 0; i <= inputNumber; i++) {
  inputArray.push(i);
  };

const roboSpeak = inputArray.map(roboger(element){
  if element.includes(1)
  return "Beep!"
})